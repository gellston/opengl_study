<UserControl x:Class="UITest.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UITest.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:convMVVM2="https://github.com/gellston/ConvMVVM2"
             convMVVM2:ViewModelLocator.AutoWireViewModel="True"
             convMVVM2:ViewModelLocator.UseNamePatternMapper="True"
             convMVVM2:ViewModelLocator.UseViewModelMapper="False"
             xmlns:behaviors="clr-namespace:UITest.Behaviors"
             x:Name="view">
    <UserControl.Resources>
        <convMVVM2:BindingProxy x:Key="Proxy" Data="{Binding ElementName=view, Path=DataContext}"></convMVVM2:BindingProxy>
    </UserControl.Resources>
    <Grid>
        <Image Source="{Binding CurrentFrame}"
               Stretch="Fill">
            <convMVVM2:Interaction.Behaviors>
                <behaviors:SizeChangedBehavior CurrentSize="{Binding Source={StaticResource Proxy}, Path=Data.CurrentSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></behaviors:SizeChangedBehavior>
                <convMVVM2:MouseEventBehavior MouseViewModel="{Binding Source={StaticResource Proxy}, Path=Data.MouseViewModel}"></convMVVM2:MouseEventBehavior>
            </convMVVM2:Interaction.Behaviors>
        </Image>
    </Grid>
</UserControl>
